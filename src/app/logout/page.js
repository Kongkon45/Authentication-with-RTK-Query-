import LogoutButton from '@/components/Logout';
import React from 'react';

const LogoutPage = () => {
    return (
        <div>
            <LogoutButton/>
        </div>
    );
};

export default LogoutPage;